import spinner from './logo.gif'

export const Loader = () => {
    return (
        <div className="loader">
            <img src={spinner} alt="loader"/>
            
        </div>
    )
}

export default Loader